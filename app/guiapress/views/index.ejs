<%- include('partials/header.ejs') %>
<%- include('partials/navbarHomepage.ejs') %>
<body>
    <div class="container">
        <div class="card border-light">
            <div class="card-body">
                <h1 class="display-4">Guiapress</h1>
                <p class="lead">Este e um projeto de blog do guia do programador feito em Nodejs</p>
            </div>
        </div>
    </div>
    <div class="container">
        <% articles.forEach(article => { %>
            <div class="card">
                <div class="card-header">
                    <h2> <%= article.title %> </h2>
                </div>
                <div class="card-body">
                    <a class="btn btn-success">Ler artigo</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
<%- include('partials/footer.ejs') %>